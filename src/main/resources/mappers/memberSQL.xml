<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hello.itemService.repository.MemberRepository">

    <update id="update">
        update MEMBER
        set member_tel=#{memberTel},
            member_birth=#{memberBirth},
            member_img=#{memberImg}
        where id = #{memberId}
    </update>

    <select id="findAll" resultType="hello.itemService.domain.Member">
        select *
        from member
    </select>

    <select id="findById" resultType="hello.itemService.domain.Member" parameterType="string">
        select *
        from member
        where member_id = #{memberId}
    </select>

    <insert id="save" parameterType="hello.itemService.domain.Member">
        insert into member(member_id, member_pwd, member_name, member_tel, member_birth, member_email)
        values (#{memberId},
                #{memberPwd},
                #{memberName},
                #{memberTel,jdbcType=VARCHAR},
                #{memberBirth,jdbcType=DATE},
                #{memberEmail})
    </insert>

    <select id="login" resultType="hello.itemService.domain.Member" parameterType="string">
        select *
        from member
        where member_id = #{memberId}
          and member_pwd = #{memberPwd}
    </select>
    <select id="findByEmail" resultType="hello.itemService.domain.Member">
        select *
        from MEMBER
        where member_EMAIL = #{memberEmail}
    </select>
</mapper>