<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hello.blogService.repository.BlogRepository">
    <insert id="insertBlog" parameterType="hello.blogService.domain.Blog">
        insert into BLOG(blog_adress,
                         blog_writer,
                         blog_name,
                         BLOG_IS_TEAM,
                         BLOG_IS_PRIVATE
                         )
        VALUES (#{blogAdress},#{blogWriter},#{blogName},#{blogIsTeam},#{blogIsPrivate})
    </insert>

    <update id="updateBlog" parameterType="hello.blogService.domain.Blog">
        update BLOG set BLOG_NAME = #{blogName},
                        blog_is_private = #{blogIsPrivate},
                        blog_explanation = #{blogExplanation},
                        blog_background_img = #{blogBackgroundImg},
                        blog_main_img = #{blogMainImg}
        where blog_adress = #{blogAdress};
    </update>
    <delete id="deleteBlog">
        update BLOG set BLOG_IS_DEL = '1' where BLOG_ADRESS = #{blogAdress}
    </delete>

    <select id="findAll" resultType="hello.blogService.domain.Blog">
        select *
        from BLOG;
    </select>
    <select id="findByBlogAdress" resultType="hello.blogService.domain.Blog">
        select *
        from BLOG
        where BLOG_ADRESS = #{blogAdress}
    </select>

</mapper>
